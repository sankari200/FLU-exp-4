# Product Billing System

A simple Flutter app that allows users to calculate the total billing amount, including GST.

## Features
- Input fields for product name, price, and quantity
- Validations for all input fields
- Auto GST calculation (5% if subtotal > ₹1000)
- Clean and minimalistic UI

## Output
Displays subtotal, GST, and total amount clearly.

## How to Run
1. Clone the repository or create a new Flutter project.
2. Replace `lib/main.dart` with the code provided.
3. Run the following commands:
   ```bash
   flutter pub get
   flutter run
